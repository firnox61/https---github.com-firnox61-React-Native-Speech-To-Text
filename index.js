/**
 * @format
 */

import {AppRegistry} from 'react-native';
import baslangic from './baslangic';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => baslangic);
